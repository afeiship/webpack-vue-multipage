## deploy this project:


## build project:
```bash
cd ~/git-ooclab/exhibition-web
npm install --registry=https://registry.npm.taobao.org
npm run build
# copy dist to `dist` dir
```

## nginx:
+ gzip
+ api proxy ? [if cros]
